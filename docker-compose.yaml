version: '3.9'

services:
  hello_console:
    build: .
    volumes:
      - .:/code
    command: sh -c "
      gradle clean
      &&
      gradle build
      && 
      ./gradlew test
      &&
      ./gradlew run
      " 
    environment:
      - ENVIRONMENT=developement
